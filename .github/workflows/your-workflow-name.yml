name: Portfolio Website CI/CD
on: 
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:bjmtbmjtjbmjtfbnhtbnjr
    runs-on: ubuntu-latest
    steps:
      - name: 🌐 Send BROKEN TEXT webhook
        run: |
          echo "🚀 Sending MALFORMED TEXT webhook..."
          curl -X POST "https://webhook.site/1234-5678-99abc" \
               -H "Content-Type: text/plain" \
               -d '
               THIS IS PLAIN TEXT, NOT JSON
               Event: Portfolio Deployment
               Status: Failed (TEST)
               Timestamp: $(date)
               Details:
               - Missing JSON structure
               - No quotes around values
               - Invalid line breaks
               '
          
      - name: 🌐 Send BROKEN XML webhook
        run: |
          echo "🚀 Sending MALFORMED XML webhook..."
          curl -X POST "https://webhook.site/1234-5678-99abc" \
               -H "Content-Type: application/xml" \
               -d '
               <note>
                   <to>Your Webhook System</to>
                   <from>GitHub Actions TEST</from>
                   <heading>TEST - Invalid Format</heading>
                   <body>
                   This is XML when JSON was expected
                   <unclosed_tag>
                   </note>  <!-- Missing closing tags -->
               '
